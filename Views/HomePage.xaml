<Page x:Class="ImdbProject.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewmodels="clr-namespace:ImdbProject.ViewModels" 
      d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Grid>
        <DataGrid ItemsSource="{Binding Titles}" 
                  SelectedItem="{Binding SelectedTitle}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title ID" Binding="{Binding TitleId}" Width="100"/>
                <DataGridTextColumn Header="Title" Binding="{Binding PrimaryTitle}" Width="*"/>
                <DataGridTextColumn Header="Runtime" Binding="{Binding RuntimeMinutes}" Width="80"/>
                <DataGridTextColumn Header="Rating" Binding="{Binding Rating.AverageRating, StringFormat='{}{0:F1}'}" Width="80"/>

                <DataGridTemplateColumn Header="Actions" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="View Details" 
                                    Command="{Binding DataContext.GoToTitleDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding TitleId}"
                                    Padding="10,5"
                                    Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
<Page x:Class="ImdbProject.Views.FavouritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FavouritesPage"
      Style="{StaticResource PageStyle}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">
            <TextBlock Text="Favourites"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="0,0,16,0" />
            <Button Content="Back to Titles"
                    Padding="8,4"
                    Click="BackToTitles_Click" />
        </StackPanel>

        <!-- Now bound to FavouriteTitles instead of Titles -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding FavouriteTitles}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title ID"
                                    Binding="{Binding TitleId}"
                                    Width="100" />
                <DataGridTextColumn Header="Title"
                                    Binding="{Binding PrimaryTitle}"
                                    Width="*" />
                <DataGridTextColumn Header="Runtime"
                                    Binding="{Binding RuntimeMinutes}"
                                    Width="80" />
                <DataGridTextColumn Header="Rating"
                                    Binding="{Binding Rating.AverageRating, StringFormat='{}{0:F1}'}"
                                    Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
